this is a file that contains commands that i've learned so far.
grep = this is used to filter files and search for files 
2.dmesg = this is used to investigate system level hardware and kernel issus
3. diff = to find differences between to files
4. chown = to change the ownership of the files and directories
5. chmod =(change file mod bits) to modify files.
6.newgrp = start a new terminal shell using developers as your active group.

Think of it as refreshing your terminal's group context.
to write scripting
first create a file with .sh extension 
write a code starts with 
#!/bin/bash or #!/bin/zsh
echo 'Hello,world'


given the permission for that file called hello.sh to execute directly without using explicit bash command".

20-08-26 
learned some new things about how arithmetic operations work in bash


"""""
	Expression for arithematic operations 
						$((expression))

#############################@21-08-2026@###################################


	we have done learning
			conditional statements in bash.
				syntax is: if [ "$a" -ep 0 ]

      AND also FOR Loop in bash :
			 Syntax:
				for var in "$@"; do
					statements
				done
      and also learned some new things
			
			"$@" is equals to all things we enter in terminal..
			
			"$#" is equals to how many we entered in terminal..
	these two are special variables in bash...


------------ARRAYS IN BASH----------------
     

      -syntax 
		- Number() -- it will crate an empty array.
		
		- ex:
		     - #!/bin/bash
			Numbers=()
			#we have to insert data in it 
			Numbers+=(10) #like this.
			echo "the numbers are ${Number[@]}"




----------basic arithmetic operations-----------

         


     you learned how to perform arithmetic operations using basic operators in Shell programming. You created a Bash script that calculates the total cost of a fruit basket by defining variables for individual costs and using arithmetic expressions to compute the total. You also learned how to make a script executable and run it from the command line.

Key points to remember:

Bash scripts start with a shebang line (#!/bin/bash).
Variables in Bash are assigned without spaces around the = sign.
Arithmetic operations in Bash are performed inside $(( )).
The echo command is used to print output.
Scripts need to be made executable with chmod +x before they can be run.


---------basic git operations-----------------


git init 

git add .
 
git commit -m " "
 

